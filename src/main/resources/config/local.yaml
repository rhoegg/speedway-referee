http:
  port: "8092"
  
anypoint:
  api:
    id: "1"
    validation: "3"
  
  app:
    clientId: "xxxxx"
    clientSecret: "yyyyy"

racedata:
  host: localhost
  port: "8093"
  protocol: HTTP
  path: /api

  
scorekeeper:
  host: localhost
  port: "8081"
  protocol: HTTP
  path: /scorekeeper-sapi

practice:
  expiry: "3600" # seconds
  
  
challenge:
  aiagent:
    speakname:
      minScore: "4"
      timeout: "20"
    question:
      timeout: "30"
    bias:
      minScore: "8"
      timeout: "60"