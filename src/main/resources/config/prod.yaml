http:
  port: "8081"
  
anypoint:
  api:
    id: "19302987"

racedata:
  host: race-data-sapi
  port: "8081"
  protocol: HTTP
  path: /api

  
scorekeeper:
  host: scorekeeper-sapi
  port: "443"
  protocol: HTTP
  path: /scorekeeper-sapi
  
challenge:
  aiagent:
    speakname:
      minScore: "4"
      timeout: "20"
    question:
      timeout: "30"
    bias:
      minScore: "100"
      timeout: "60"